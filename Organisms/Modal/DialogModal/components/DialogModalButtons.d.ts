import React, { ReactElement } from "react";
export interface Props {
    children: ReactElement;
}
declare const DialogModalButtons: ({ children, }: Props) => React.JSX.Element;
export default DialogModalButtons;
