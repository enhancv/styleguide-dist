import React from "react";
export interface Props {
    description: string;
}
declare const PopupModalDescription: ({ description }: Props) => React.JSX.Element;
export default PopupModalDescription;
